package cn.com.mobnote.golukmobile.thirdshare;

import android.support.v4.app.Fragment;

public class CustomShareUtil extends Fragment {

	public void openShareWindow() {
		/**
		 * 调用postShare分享。跳转至分享编辑页，然后再分享。</br> [注意]<li>
		 * 对于新浪，豆瓣，人人，腾讯微博跳转到分享编辑页，其他平台直接跳转到对应的客户端
		 */
		// CustomShareBoard shareBoard = new CustomShareBoard(getActivity());
		// shareBoard.showAtLocation(getActivity().getWindow().getDecorView(),
		// Gravity.BOTTOM, 0, 0);
	}
}
