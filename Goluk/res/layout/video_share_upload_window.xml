<?xml version="1.0" encoding="utf-8"?>
<!-- 视频上传window -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="30dp"
    android:background="#d0000000"
    android:clickable="false" >

    <!-- 上传进度 -->

    <ProgressBar
        android:id="@+id/progress_horizontal"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_alignParentBottom="true"
        android:background="#FFFFFF"
        android:max="100"
        android:progress="50"
        android:progressDrawable="@drawable/progress_horizontal" />

    <TextView
        android:id="@+id/video_upload_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:text="@string/share_video_upload_text"
        android:textColor="#ffffff"
        android:textSize="12sp" />

    <ImageView
        android:id="@+id/video_loading_img"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginRight="10dp"
        android:layout_toLeftOf="@id/video_upload_text"
        android:background="@drawable/tips_go"
        android:contentDescription="@string/imgdes" />

    <TextView
        android:id="@+id/video_upload_percent"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerVertical="true"
        android:layout_marginLeft="10dp"
        android:layout_toRightOf="@id/video_upload_text"
        android:textColor="#ffffff"
        android:textSize="12sp" />

</RelativeLayout>